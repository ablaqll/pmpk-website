// Default client configuration - single organization
// Note: ID must be string to match database schema (pgText/sqliteText)
export const DEFAULT_CLIENT = {
  id: '1',
  slug: 'pmpk9',
  name: 'ПМПК №9',
  logoUrl: null
};

export const DEFAULT_CLIENT_ID = DEFAULT_CLIENT.id;
export const DEFAULT_CLIENT_SLUG = DEFAULT_CLIENT.slug;

