# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run db:push || echo 'DB push will run on first deploy'"]

[start]
cmd = "npm run start"


